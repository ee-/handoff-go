Read the repository root `AGENTS.md` and trusted Handoff Go governance.

Act as the configured Coder.

The workspace has been prepared on the exact target branch and head for the
discovered Coder-owned Work Order.

Enforce the Handoff Go Security Gate.

Implement the next authorized bounded transition for this Work Order by editing
files in the workspace.

Do not commit, push, or open PRs. Workspace edits will be captured as a patch
and persisted by the authorized persistence job.
